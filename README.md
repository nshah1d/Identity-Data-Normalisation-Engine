# Contacts-Hub
A self-hosted web app for visualizing .vcf and .csv contact libraries. Features a custom regex engine for handling complex Apple/iCloud metadata, a modern 3-pane dark UI, and zero dependencies (Vanilla JS + PHP).
